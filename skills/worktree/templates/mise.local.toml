# Per-worktree overrides. Gitignored. Do not commit.
# Generated by the `worktree` skill / `mise run worktree:new`.

[env]
DB_NAME      = "{{REPO}}_{{SLUG_UNDERSCORED}}_development"
TEST_DB_NAME = "{{REPO}}_{{SLUG_UNDERSCORED}}_test"
REDIS_URL    = "redis://localhost:6388/{{REDIS_INDEX}}"
